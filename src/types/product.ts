export type SizesType = {
	id?: number;
	label: string;
	isActive?: boolean;
};

export type ColorsType = {
	label: string;
	value: string;
	disabled?: boolean;
	isActive?: boolean;
};

export type ProductSliderDataType = {
	id?: number;
	label: string;
	imageUrl: string;
};
